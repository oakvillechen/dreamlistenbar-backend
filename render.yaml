services:
  - type: web
    name: dreamlistenbar-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm install -D playwright && npx playwright install chromium
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
