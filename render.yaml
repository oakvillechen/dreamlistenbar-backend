services:
  - type: web
    name: dreamlistenbar-backend
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 3001
    healthCheckPath: /api/search
