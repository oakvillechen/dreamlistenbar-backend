run = "npm start"
entrypoint = "server.js"

[env]
PORT = "3001"
NODE_ENV = "production"

[[ports]]
localPort = 3001
externalPort = 80

[installer]
command = "npm install && npx playwright install chromium"
